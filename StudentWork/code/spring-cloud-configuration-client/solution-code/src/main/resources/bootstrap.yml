#-----------------------------------------------------------------------------#
# BOOTstrap Properties #
# inside ./src/main/resources/bootstrap.yml
#-----------------------------------------------------------------------------#
spring:
  cloud:
    config:
      uri: http://user1:s3cr3t@localhost:8888
  application:
    name: config-client
  profiles:
    active: development

# Turn off security for this app, for now.
management:
  security:
    enabled: false
security:
  basic:
    enabled: false

# Create BASIC-Authentication credentials
  user:
    name: user1
    password: s3cr3t

#-----------------------------------------------------------------------------#
